import { ClickerGame } from "@/components/ClickerGame";

export const metadata = {
  title: "Financial Freedom Clicker — WorthWise Arcade",
  description: "Build your net worth one click at a time. Buy assets, earn passive income.",
};

export default function ClickerPage() {
  return <ClickerGame />;
}
